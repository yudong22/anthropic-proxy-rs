{
  "name": "proxy-rs",
  "version": "1.3.0",
  "private": true,
  "description": "Anthropic API 到 OpenAI 兼容端点的代理（Tauri 2 桌面应用 + Rust 代理核心）",
  "license": "MIT",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build -b app",
    "build:dmg": "tauri build -b dmg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.5"
  }
}
